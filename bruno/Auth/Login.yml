info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{process.env.APP_URL}}/api/login"
  body:
    type: json
    data: |-
      {
        "email": "test@example.com",
        "password": "password"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
