info:
  name: Destroy
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{process.env.APP_URL}}/api/tasks/4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
