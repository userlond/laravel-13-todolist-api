info:
  name: Index
  type: http
  seq: 1

http:
  method: GET
  url: "{{process.env.APP_URL}}/api/tasks?filter[is_done]=1&sort=-title"
  params:
    - name: filter[is_done]
      value: "1"
      type: query
    - name: sort
      value: -title
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
