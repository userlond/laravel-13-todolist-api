info:
  name: Index
  type: http
  seq: 1

http:
  method: GET
  url: "{{process.env.APP_URL}}/api/tasks"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
