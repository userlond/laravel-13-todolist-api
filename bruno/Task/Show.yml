info:
  name: Show
  type: http
  seq: 2

http:
  method: GET
  url: "{{process.env.APP_URL}}/api/tasks/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
