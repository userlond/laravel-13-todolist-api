info:
  name: Update
  type: http
  seq: 4

http:
  method: PUT
  url: "{{process.env.APP_URL}}/api/tasks/1"
  body:
    type: json
    data: |-
      {
        "title": "Task 1 edit",
        "is_done": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
